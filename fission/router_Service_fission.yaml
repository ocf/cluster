apiVersion: v1
kind: Service
metadata:
  labels:
    application: fission-router
    chart: fission-all-v1.21.0
    svc: router
  name: router
spec:
  ports:
  - port: 80
    targetPort: 8888
  selector:
    svc: router
  type: ClusterIP
