apiVersion: apps/v1
kind: Deployment
metadata:
  name: rt
spec:
  replicas: 1
  selector:
    matchLabels:
      transpire.ocf.io/deployment: rt
  template:
    metadata:
      labels:
        transpire.ocf.io/deployment: rt
    spec:
      containers:
      - env: []
        envFrom:
        - secretRef:
            name: keycloak-secret
        image: ghcr.io/ocf/rt:43cc2fdf5900767b428091384bd869741677f2e3
        imagePullPolicy: IfNotPresent
        name: rt-nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: /opt/share/secrets/rt
          name: secrets
      dnsConfig:
        searches:
        - ocf.berkeley.edu
      dnsPolicy: ClusterFirst
      volumes:
      - name: secrets
        secret:
          secretName: keycloak-secret
