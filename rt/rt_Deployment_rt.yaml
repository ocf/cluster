apiVersion: apps/v1
kind: Deployment
metadata:
  name: rt
spec:
  replicas: 1
  selector:
    matchLabels:
      transpire.ocf.io/deployment: rt
  template:
    metadata:
      labels:
        transpire.ocf.io/deployment: rt
    spec:
      containers:
      - env: []
        envFrom:
        - secretRef:
            name: keycloak-env
        image: ghcr.io/ocf/rt:29915174ac13db5450dadc632ca0e189c4e8efd9
        imagePullPolicy: IfNotPresent
        name: rt-nginx
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: /opt/share/secrets/rt
          name: secrets
      dnsConfig:
        searches:
        - ocf.berkeley.edu
      dnsPolicy: ClusterFirst
      volumes:
      - name: secrets
        secret:
          secretName: keycloak-secret
