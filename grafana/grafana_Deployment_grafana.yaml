apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      transpire.ocf.io/deployment: grafana
  template:
    metadata:
      labels:
        transpire.ocf.io/deployment: grafana
    spec:
      containers:
      - env: []
        envFrom:
        - configMapRef:
            name: grafana
        - secretRef:
            name: grafana
        image: ghcr.io/ocf/grafana:07afbc883f7587e2c1961e25af8888f99e3923de
        imagePullPolicy: IfNotPresent
        name: main
        ports:
        - containerPort: 3000
