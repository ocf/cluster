apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      transpire.ocf.io/deployment: grafana
  template:
    metadata:
      labels:
        transpire.ocf.io/deployment: grafana
    spec:
      containers:
      - env: []
        envFrom:
        - configMapRef:
            name: grafana
        - secretRef:
            name: grafana
        image: ghcr.io/ocf/grafana:40851be2fd248402078b84dff451dbb2907196fb
        imagePullPolicy: IfNotPresent
        name: main
        ports:
        - containerPort: 3000
