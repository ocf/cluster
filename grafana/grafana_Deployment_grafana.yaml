apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      transpire.ocf.io/deployment: grafana
  template:
    metadata:
      labels:
        transpire.ocf.io/deployment: grafana
    spec:
      containers:
      - env: []
        envFrom:
        - configMapRef:
            name: grafana
        - secretRef:
            name: grafana
        image: ghcr.io/ocf/grafana:68d3928c1a1ec41e77dd2c9eeae3b9e4591d7d87
        imagePullPolicy: IfNotPresent
        name: main
        ports:
        - containerPort: 3000
