apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      transpire.ocf.io/deployment: grafana
  template:
    metadata:
      labels:
        transpire.ocf.io/deployment: grafana
    spec:
      containers:
      - env: []
        envFrom:
        - configMapRef:
            name: grafana
        - secretRef:
            name: prometheus-auth
        image: ghcr.io/ocf/grafana:7d95e20e5f8ab2465eac100d729507549ba763d5
        imagePullPolicy: IfNotPresent
        name: main
        ports:
        - containerPort: 3000
