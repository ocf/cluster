apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: grafana
spec:
  keys:
  - PROMETHEUS_AUTH_PASSWORD
  - GF_DATABASE_PASSWORD
  - GF_SECURITY_ADMIN_PASSWORD
  - GF_SESSION_PROVIDER_CONFIG
  - GF_AUTH_GENERIC_OAUTH_CLIENT_SECRET
  path: kvv2/grafana/grafana
  type: Opaque
