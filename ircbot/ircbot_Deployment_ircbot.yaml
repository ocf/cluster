apiVersion: apps/v1
kind: Deployment
metadata:
  name: ircbot
spec:
  replicas: 1
  selector:
    matchLabels:
      transpire.ocf.io/deployment: ircbot
  template:
    metadata:
      labels:
        transpire.ocf.io/deployment: ircbot
    spec:
      containers:
      - image: harbor.ocf.berkeley.edu/ocf/ircbot/ircbot:540aae3df2d7c995074cb0b67150df5bc13a770c
        imagePullPolicy: IfNotPresent
        name: main
        ports:
        - containerPort: 8888
        volumeMounts:
        - mountPath: /etc/ocf-ircbot
          name: config
      dnsConfig:
        searches:
        - ocf.berkeley.edu
      dnsPolicy: ClusterFirst
      volumes:
      - name: config
        secret:
          secretName: ircbot
