apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: teleport
  namespace: teleport
spec:
  dnsNames:
  - tele.ocf.io
  - '*.tele.ocf.io'
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt
  secretName: teleport-tls
