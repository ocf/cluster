apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: teleport
  name: teleport
  namespace: teleport
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
