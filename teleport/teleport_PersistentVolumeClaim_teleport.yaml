apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/component: auth
    app.kubernetes.io/instance: teleport
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: teleport-cluster
    app.kubernetes.io/version: 17.7.1
    helm.sh/chart: teleport-cluster-17.7.1
    teleport.dev/majorVersion: '17'
  name: teleport
  namespace: teleport
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
